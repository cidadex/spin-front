export const enum CalculationResultEnum {
  Pardonable = "PARDONABLE",
  Commutable = "COMMUTABLE",
  NoReduction = "NO_REDUCTION",
  PendingInfo = "PENDING_INFO",
}

export const enum CalculationStatusEnum {
  Open = "OPEN",
  InProgress = "IN_PROGRESS",
  Completed = "COMPLETED",
  Canceled = "CANCELED",
}

export const enum CalculationVariavelEscopoEnum {
  Apenado = "apenado",
  Pena = "pena",
}

export const enum CalculationVariavelTypeEnum {
  Boolean = "boolean",
  List = "list",
  Integer = "integer",
  Decimal = "decimal",
}
